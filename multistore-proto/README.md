# 予約GO 多店舗管理プロトタイプ（2026-09-23）

開発部 川島さんのモック ph.4（`20260908_川島さんモック`）をコピーし、本部（グループ）側の画面を足したもの。
**画面の見た目・動きの確認用で、データはすべて架空のダミー**（`js/hq_data.js`）。サーバー処理は無い。

## 開き方

| 見たいもの | 開くファイル |
|---|---|
| 本部コンソール（入口） | `B画面(管理画面)/hq_index.html` |
| 店舗画面（店舗切替つき） | 本部の「店舗別の比較」で店舗名を押す／`B画面(管理画面)/admin_index.html?store=s1001` |
| お客様向けグループ予約ページ | `C画面(予約フォーム)/store_select.html` |

ブラウザで直接開けば動く（ビルド不要）。

**閲覧にはPINコードが必要**（`pin_gate.js`。PINはハッシュで照合し、平文はソースに置いていない。PINは商品企画の担当者に確認する）。
一度通ればタブを閉じるまで再入力は不要。公開URL：https://dat0925.github.io/yoyakugo/multistore-proto/

## 追加・変更したファイル

| ファイル | 役割 |
|---|---|
| `B画面(管理画面)/hq_index.html` | M2 全店ダッシュボード |
| `B画面(管理画面)/hq_reservations.html` | M4 横断予約一覧 |
| `B画面(管理画面)/hq_stores.html` | M1 店舗管理・店舗追加 |
| `B画面(管理画面)/hq_distribute.html` | M5 一括配信＋項目ロック |
| `B画面(管理画面)/hq_users.html` | M6 ユーザー・権限（4ロール） |
| `B画面(管理画面)/hq_billing.html` | M8 一括請求・店舗別明細 |
| `B画面(管理画面)/hq_customers.html` | S1 顧客台帳（次期リリースの見せ方だけ） |
| `B画面(管理画面)/js/hq_data.js` | ダミーデータ（店舗・ユーザー・権限・配信テンプレート・料金案） |
| `B画面(管理画面)/js/hq_shell.js` | 本部画面の共通の枠（サイドメニュー・上部バー） |
| `B画面(管理画面)/js/store_switcher.js` | 既存の店舗画面に「本部へ戻る」・店舗切替・ロック案内を差し込む |
| `B画面(管理画面)/hq.css` | 本部画面の追加スタイル（`hq-` `ss-` 接頭辞。既存の style.css の変数を使う） |
| `C画面(予約フォーム)/store_select.html` | M7 グループ予約ページ |
| `pin_gate.js` | 全HTMLの `<head>` 先頭で読み込むPINゲート |
| 既存 `admin_*.html` 17ファイル | `</head>` の直前に読み込み4行（hq.css・hq_data.js・store_switcher.js）を追加しただけ |
| 既存 `C画面(予約フォーム)/index.html` | `?store=` のときだけ院名と「他の院を選ぶ」を出すスクリプトを末尾に追加 |

## 設計の考え方

- **店舗の画面は作り直さない。** 本部の階層を上に1つ足し、店舗画面には差し込みだけで入る（既存12契約への影響を最小にする）
- **主役は一括配信。** 調査した競合で「本部から設定を配って店舗で上書き可否を決める」を打ち出すサービスは無かった
- **画面・見た目・動き・データを分けた。** 引き継ぐときに機械変換が要らないように（第2の脳「プロトタイプをAIに作らせるときの前提指示」）

要件定義と説明資料：`C:\Users\1090163\OneDrive - 株式会社EPARK\商品企画\予約GO\多店舗管理_20260923\`
